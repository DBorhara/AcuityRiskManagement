import React from 'react';
import { Link } from 'react-router-dom';
import './footer.styles.scss';

const footer = () => (
  <div className="main-footer">
    <div className="container">I am the footer</div>
  </div>
);

export default footer;
